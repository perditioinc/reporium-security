# Local substrate config. Copy to .env (optional) and adjust if needed.
# All values are local-only / $0. No secrets.

# Port the PyPI JSON stub listens on inside its container (HTTPS).
OSV_PORT=443

# Path to the seeded vulnerability fixture inside the stub container.
OSV_SEED=/data/vulns.json
